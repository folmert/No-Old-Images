{
  "manifest_version": 3,
  "name": "No Old Images",
  "description": "Simplifies searching for fresh content by generating CSS to hide already displayed images. You can paste the CSS into a style manager (i.e. Stylus). Currently works only with Google Images.",
  "version": "1.0",
  "action": {
    "default_icon": "icon.jpg",
    "default_popup": "popup.html",
    "default_title": "No Old Images"
  },
  "permissions": [
    "activeTab",
    "tabs",
    "scripting",
    "storage"
  ],
}
